# B站视频明星人脸识别浏览器插件 - 需求文档

> 版本：1.0  
> 作者：你的名字  
> 日期：2026-08-30  
> 状态：草稿

---

## 1. 项目概述

### 1.1 项目背景
用户在观看B站视频（如综艺、访谈、影视剪辑）时，常对画面中出现的演员、歌手等明星感到好奇，但手动搜索费时费力。本项目旨在开发一款浏览器插件，自动识别视频中的中国明星，并实时显示其姓名，提升观看体验。

### 1.2 项目目标
- **核心目标**：构建一个Chrome浏览器插件，在B站视频播放过程中自动检测、识别人脸，并叠加显示明星名字。
- **范围**：MVP阶段仅支持B站（www.bilibili.com）视频页面，识别对象为100~500位中国知名明星。
- **非目标**：不支持其他视频网站，不支持动态更新明星库（初期为本地固定库）。

### 1.3 用户角色
- **普通观众**：观看视频，直接受益于识别结果。
- **开发者（维护者）**：负责更新明星数据库和插件维护。

---

## 2. 产品需求（PRD）

### 2.1 功能需求

| 编号 | 模块 | 需求描述 | 优先级 |
|------|------|----------|--------|
| FR-1 | 视频帧捕获 | 插件能够获取B站视频播放器的当前画面（帧） | P0 |
| FR-2 | 人脸检测 | 从捕获的帧中检测出所有人脸位置（边界框） | P0 |
| FR-3 | 人脸跟踪 | 对连续帧中的同一人脸进行跟踪，减少重复识别计算（可选，但推荐） | P1 |
| FR-4 | 人脸识别 | 将检测到的人脸与本地明星特征库比对，给出最佳匹配的名字 | P0 |
| FR-5 | 结果展示 | 在视频画面上，于对应人脸位置上方绘制名字标签（中文名） | P0 |
| FR-6 | 数据库管理 | 插件需内嵌或加载明星特征数据库（含头像特征向量和姓名） | P0 |
| FR-7 | 开关控制 | 提供插件开关，允许用户随时启用/禁用识别功能 | P2 |
| FR-8 | 错误处理 | 识别失败（未知脸）时显示“？”或“未知”，不影响观看 | P1 |
| FR-9 | 服务状态检测 | 当本地识别服务未启动时，插件给出友好提示，不崩溃 | P1 |

### 2.2 非功能需求

| 编号 | 需求 | 详细说明 |
|------|------|----------|
| NFR-1 | 性能 | 处理速度 ≥ 5帧/秒（fps），保持视频播放流畅 |
| NFR-2 | 兼容性 | 兼容Chrome 88+，支持Windows/macOS |
| NFR-3 | 隐私 | 所有图像数据不离开用户设备（通过本地服务处理） |
| NFR-4 | 可维护性 | 明星数据库可独立更新，无需重装插件 |
| NFR-5 | 可用性 | 界面简洁，名字标签清晰易读，且不遮挡重要视频内容 |
| NFR-6 | 资源占用 | 在主流配置PC（i5 CPU, 8GB RAM）上，CPU占用 ≤ 30% |

---

## 3. 技术需求（TRD）

### 3.1 技术选型与架构

**整体架构**：Chrome插件（前端） + Python本地识别服务（后端）

- **前端（插件）**：JavaScript, HTML, CSS → 捕获视频帧、发送请求、渲染结果。
- **后端（本地服务）**：Python + Flask → 加载InsightFace模型，执行人脸检测与识别。
- **数据库**：明星特征向量文件（`.pkl`），存放于本地。

**为什么选择混合架构？**  
- 浏览器中运行深度模型（TensorFlow.js/Wasm）虽然可行，但模型精度和性能受限，部署复杂。  
- 利用本地Python服务可以使用成熟的InsightFace库，识别精度高，开发速度快。

**通信方式**：HTTP RESTful API，插件将帧图片（base64编码）发送至本地服务，服务返回人脸框及明星名字。

### 3.2 系统架构图

```plaintext
+---------------------------+         HTTP (JSON)          +----------------------------+
|   Chrome 插件 (前端)      |  -------- 帧图像 --------->  |  本地识别服务 (Python)      |
| - 捕获视频帧              | <------- 结果 (人名+框) ----  | - 人脸检测 (RetinaFace)     |
| - 绘制标签                |                               | - 特征提取 (InsightFace)    |
| - 用户交互                |                               | - 特征比对 (余弦相似度)     |
+---------------------------+                               +----------------------------+
                                                                    |
                                                                    v
                                                          +----------------------------+
                                                          |  明星特征库 (.pkl)          |
                                                          |  姓名 + 特征向量 (512维)    |
                                                          +----------------------------+

3.3 模块详细设计
3.3.1 Chrome插件模块
Manifest v3：manifest.json 声明权限：activeTab, storage, scripting, host_permissions（允许向 http://127.0.0.1 发送请求）。

Content Script：注入到B站视频页面，利用 MutationObserver 检测播放器加载，获取 <video> 元素。

帧捕获：使用 canvas 定时截取视频帧（例如每200ms），转换为base64图片（可压缩质量）。

通信：发送POST请求到 http://localhost:5000/recognize，携带图片数据。

结果渲染：接收返回的 [{bbox, name}] 数组，利用DOM创建绝对定位的 <div> 标签，覆盖在视频上方，并随视频缩放而更新位置。

开关：通过浏览器工具栏的弹出窗口（popup）控制启用/禁用，使用 chrome.storage 存储状态。

3.3.2 本地识别服务（Python Flask）
模型加载：使用 insightface.app.FaceAnalysis 加载 buffalo_l 模型（包含检测和识别）。

特征库加载：启动时从 star_features.pkl 加载所有明星的名字及其512维特征向量（已归一化）。

API端点：POST /recognize，接收JSON {"image": "data:image/jpeg;base64,..."}。

处理流程：

解码base64为OpenCV图像（BGR）。
调用 face_analysis.get(img) 获得所有人脸，每人返回 bbox 和 embedding。
对每个 embedding，计算与特征库中所有向量的余弦相似度。
若最高相似度 > 阈值（如0.6），则匹配该明星，否则返回 "Unknown"。
返回JSON {"faces": [{"bbox": [x1,y1,x2,y2], "name": "张三"}]}。
性能优化：可使用多线程或异步处理；缓存最近帧的检测结果，避免重复计算。

3.3.3 明星特征库构建
数据源：采用 “104位明星人脸识别数据集” 起步（可后期扩展至CN-Celeb）。

构建脚本（build_db.py）：

遍历按人名分组的图片文件夹。

使用同一 FaceAnalysis 模型提取每个人脸特征，计算平均向量。

保存为 star_features.pkl（字典：{name: embedding_array}）。

数据格式：embedding 为numpy数组，归一化处理（便于余弦相似度计算）。

3.4 接口定义
请求（插件 → 服务）
text
POST /recognize
Content-Type: application/json
{
  "image": "data:image/jpeg;base64,/9j/4AAQ..."
}
响应（服务 → 插件）
text
HTTP 200 OK
{
  "faces": [
    {
      "bbox": [100, 50, 200, 180],   // 左、上、右、下（像素）
      "name": "周杰伦"
    },
    ...
  ],
  "processing_time": 0.23  // 可选
}
错误响应
text
HTTP 500
{
  "error": "Model not loaded"
}
3.5 本地数据库设计
文件：star_features.pkl（使用 pickle 或 joblib 保存）。

结构：dict，键为 str 姓名，值为 numpy.ndarray 形状 (512,)，dtype=float32。

3.6 开发环境与工具
组件	工具/版本
Python	3.8+
Python依赖	insightface, onnxruntime, opencv-python, flask, numpy, pickle
前端	Chrome浏览器 (88+)，文本编辑器 (VSCode)
插件打包	无需打包，加载已解压的扩展程序
版本控制	Git
4. 实施步骤（开发计划）
阶段一：环境搭建与明星数据库准备（1~2天）
安装Python 3.8+，创建虚拟环境。

安装依赖库（pip install insightface onnxruntime opencv-python flask numpy）。

下载“104位明星人脸识别数据集”或自行收集明星照片（每人10~20张），按姓名分文件夹存放。

编写 build_db.py 脚本，加载InsightFace模型，提取每张图片的特征，计算平均特征，保存为 star_features.pkl。

测试：用单张图片验证特征提取和比对是否正确。

阶段二：本地识别服务开发（2~3天）
创建Flask应用，编写 /recognize 路由。

集成模型加载、图像解码、人脸检测、特征比对逻辑。

编写单元测试，用测试图片验证API返回正确的姓名和框坐标。

启动服务，确保 http://localhost:5000/recognize 可正常访问。

阶段三：Chrome插件开发（3~4天）
创建插件目录，编写 manifest.json（v3），配置权限。

编写 content.js 脚本：

检测B站视频播放器DOM（#bilibili-player video）。

定时截取视频帧（canvas.drawImage(video)），转为base64。

发送POST请求至本地服务，处理返回结果。

动态创建覆盖层 <div> 标签显示名字，定位在人脸框上方。

监听视频缩放/全屏事件，更新标签位置。

编写 popup.html 和 popup.js，实现启用/禁用开关（使用 chrome.storage）。

阶段四：联调与优化（2~3天）
启动本地服务，加载插件，访问B站视频进行端到端测试。

调整识别阈值（相似度）和采样频率（帧间隔），平衡准确率和性能。

处理边缘情况（无人脸、多人脸、识别失败、服务未启动）。

优化图片压缩和网络传输，降低延迟。

压力测试：连续播放视频10分钟，观察内存和CPU占用。

阶段五：测试与文档（1周）
在不同类型的B站视频（综艺、电影、访谈）中测试，记录识别准确率和速度。

撰写毕业设计论文，包含系统设计、核心代码、测试结果分析。

准备演示视频和PPT。

5. 测试计划
测试项	测试内容	预期结果
功能测试	播放包含已知明星的视频	正确显示名字标签
功能测试	播放不含明星的视频（或明星未入库）	不显示名字或显示“未知”
功能测试	同时出现多人脸	每个检测到的人脸都显示名字或“未知”
性能测试	播放1080p视频，观察CPU占用	≤ 30% (i5 CPU)
性能测试	连续运行10分钟	无内存泄漏，稳定
兼容性测试	不同分辨率、全屏模式	标签位置正确，不偏移
可靠性测试	服务未启动时打开B站视频	插件显示“服务未连接”，不崩溃
可靠性测试	服务中途停止	插件自动重连或提示
6. 风险评估与应对
风险	影响	应对措施
明星数据集质量差（角度单一）	识别准确率低	增加更多角度照片，或使用数据增强；调低相似度阈值
B站页面结构更新导致插件失效	功能不可用	使用更稳定的选择器，或提供手动配置播放器元素的功能
本地服务启动复杂，用户难安装	用户接受度低	提供一键启动脚本（.bat/.sh），并在插件中检测服务状态，给出引导
性能不足（识别速度慢）	体验差	降低截帧频率（如500ms），或仅在场景变化时触发识别；使用GPU加速（如CUDA）
隐私问题	用户担忧	明确告知所有处理在本地完成，不上传任何数据
7. 附录
7.1 数据集推荐
“104位明星人脸识别数据集”：百度AI Studio或GitHub搜索，规模小，适合快速启动。

备选：自行爬取20~30位常见明星照片（每人10~20张）。

7.2 参考文献
InsightFace官方GitHub: https://github.com/deepinsight/insightface

Chrome Extension开发文档: https://developer.chrome.com/docs/extensions/

Flask官方文档: https://flask.palletsprojects.com/

7.3 术语表
术语	解释
Embedding	人脸特征向量，用于表示人脸身份的512维浮点数数组
余弦相似度	衡量两个向量方向一致性的指标，范围[-1,1]，用于特征比对
MVP	最小可行性产品，仅包含核心功能
Wasm	WebAssembly，一种可在浏览器中高效运行的二进制指令格式
8. 版本历史
版本	日期	修改内容	作者
1.0	2026-08-30	初始版本	你的名字

